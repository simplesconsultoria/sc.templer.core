Changelog
----------

#set $header_text = "%sdev (unreleased)" % $version
#set $header_line = "^" * len($header_text)
$header_text
$header_line

 *  Package created using templer
    [${$author or None}]